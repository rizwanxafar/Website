// src/app/icons-test/page.js
import { Search, Moon, Sun, ChevronRight } from "lucide-react";

export default function IconsTestPage() {
  return (
    <div className="min-h-screen mx-auto max-w-6xl px-3 sm:px-4 lg:px-6 py-6">
      <h1 className="text-2xl font-semibold mb-4">Icon test (Lucide)</h1>

      {/* Default size (20px) */}
      <div className="flex items-center gap-3 mb-6">
        <Search className="w-5 h-5" strokeWidth={1.5} />
        <span>Default (20px): <code>className="w-5 h-5"</code>, <code>strokeWidth=1.5</code></span>
      </div>

      {/* Larger size (24px) */}
      <div className="flex items-center gap-3 mb-6">
        <Search className="w-6 h-6" strokeWidth={1.5} />
        <span>Larger (24px): <code>className="w-6 h-6"</code></span>
      </div>

      {/* Example button with icon */}
      <button className="inline-flex items-center gap-2 rounded px-4 py-2 border border-neutral-300 hover:bg-neutral-50 transition">
        Continue <ChevronRight className="w-5 h-5" strokeWidth={1.5} />
      </button>

      {/* Theme toggle icon pair (just visual) */}
      <div className="mt-8 flex items-center gap-4">
        <Sun className="w-5 h-5" strokeWidth={1.5} />
        <Moon className="w-5 h-5" strokeWidth={1.5} />
        <span>Use one of these in your existing theme toggle later.</span>
      </div>
    </div>
  );
}
